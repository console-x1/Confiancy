<!DOCTYPE html>
<html lang="fr">

<head>
    <meta charset="UTF-8">
    <title>Vérification de l'email</title>
    <link rel="icon" href="../media/logoCircle.png">
    <style>
        body {
            background-color: #121212;
            color: #f0f0f0;
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
        }

        .container {
            max-width: 500px;
            margin: 5vh auto;
            background-color: #1e1e1e;
            padding: 2rem;
            border-radius: 8px;
            box-shadow: 0 0 15px rgba(0, 0, 0, 0.6);
        }

        h1 {
            color: #ff9800;
            margin-bottom: 1rem;
        }

        p {
            line-height: 1.5;
        }

        .highlight {
            color: #4cafef;
            font-weight: bold;
        }

        .time {
            font-size: 0.9rem;
            color: #bbb;
        }

        .spam-warning {
            margin-top: 1.5rem;
            padding: 1rem;
            background-color: #2a2a2a;
            border-left: 4px solid #ff9800;
            font-size: 0.95rem;
        }

        form {
            margin-top: 1.5rem;
        }

        label {
            display: block;
            margin-top: 1rem;
            margin-bottom: 0.3rem;
            font-weight: bold;
        }

        input {
            width: 100%;
            padding: 0.75rem;
            border: none;
            border-radius: 4px;
            font-size: 1rem;
            background-color: #2a2a2a;
            color: #f0f0f0;
            letter-spacing: 1px;
        }

        input::placeholder {
            color: #888;
        }

        small {
            display: block;
            margin-top: 0.3rem;
            font-size: 0.85rem;
        }

        button,
        a.button {
            display: inline-block;
            margin-top: 1.5rem;
            padding: 0.75rem 1.5rem;
            background-color: #4cafef;
            color: #121212;
            border: none;
            border-radius: 4px;
            font-weight: bold;
            cursor: pointer;
            font-size: 1rem;
            text-decoration: none;
            text-align: center;
        }

        button:hover,
        a.button:hover {
            background-color: #3a9bdc;
        }

        a.button.secondary {
            background-color: #ff9800;
        }

        a.button.secondary:hover {
            background-color: #e68900;
        }
    </style>
</head>

<body>
    <div class="container">
        <h1>Modification du mot de passe</h1>
        <p>Un code de vérification a été envoyé à :</p>
        <p class="highlight">
            <%= email %>
        </p>
        <p class="time">Envoyé il y a <%= timeCode %>minutes - Valide 30minutes</p>

        <div class="spam-warning">
            📩 Si vous ne recevez rien dans quelques minutes, pensez à vérifier votre dossier <strong>Spam / Courrier
                indésirable</strong>.
        </div>

        <div id="verifyContainer">
            <input type="hidden" id="email" value="<%= email %>">

            <label for="username">Choisissez un nouveau mot de passe :</label>
            <input type="password" id="password" minlength="12" placeholder="Nouveau mot de passe" required>

            <label for="code">Code de vérification :</label>
            <input type="text" id="code" placeholder="Entrez votre code à 12 chiffres" minlength="12" maxlength="12" required>

            <button id="verifyBtn">Valider</button>
        </div>


        <button type="button" id="resendBtn" class="button secondary">Renvoyer le code</button>

    </div>

    <script>
        document.getElementById('resendBtn').addEventListener('click', async () => {
            try {
                const res = await fetch('/api/auth/sendResetPasswordCode', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({ email: "<%= email %>" })
                });

                const data = await res.json();

                if (data.success) {
                    alert("✅ Code renvoyé avec succès !");
                } else {
                    alert("❌ Erreur : " + (data.message ?? "Impossible de renvoyer le code (maximum un envois par minute!)"));
                }
            } catch (err) {
                alert("⚠️ Erreur réseau", err);
                console.log(err)
            }
        });
    </script>
    <script>
        document.getElementById('verifyBtn').addEventListener('click', async () => {
            const email = document.getElementById('email').value.trim();
            const password = document.getElementById('password').value.trim();
            const code = document.getElementById('code').value.trim();

            if (!password || !code) {
                alert("Veuillez remplir tous les champs");
                return;
            }

            try {
                const res = await fetch('/api/auth/update-password', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({ email, password, code })
                });

                const data = await res.json();

                if (data.success) {
                    window.location.href = data.redirect;
                } else {
                    alert("❌ Erreur : " + (data.message || "Vérification échouée"));
                }
            } catch (err) {
                console.error(err);
                alert("⚠️ Erreur réseau");
            }
        });
    </script>
</body>

</html>