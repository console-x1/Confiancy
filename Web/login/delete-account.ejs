<!DOCTYPE html>
<html lang="fr">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Delete account</title>
    <link rel="icon" href="../media/logoCircle.png">

    <style>
        html {
            background-color: #131b2d;
            z-index: -1;
        }
    </style>

    <script src="../javascript/tailwind.min.js"></script>

    <style type="text/tailwindcss">
        @layer base {
            body {
                font-family: 'Inter', sans-serif;
                min-height: 100vh;
            }
                
            ::-webkit-scrollbar {
                width: 6px;
            }
                
            ::-webkit-scrollbar-track {
                background: transparent;
            }
                
            ::-webkit-scrollbar-thumb {
                background: rgba(59, 130, 246, 0.3);
                border-radius: 3px;
            }
                
            ::-webkit-scrollbar-thumb:hover {
                background: rgba(59, 130, 246, 0.5);
            }
                
            * {
                scrollbar-width: thin;
                scrollbar-color: rgba(59, 130, 246, 0.3) transparent;
            }
                
            .trust-badge {
                background: linear-gradient(135deg, #3b82f6 0%, #1d4ed8 100%);
            }
        }
    </style>
</head>

<body class="min-h-screen flex items-center justify-center p-10">

    <div class="fixed inset-0 -z-0 pointer-events-none">
        <div
            class="absolute top-0 left-0 w-32 h-32 bg-blue-500 rounded-full mix-blend-multiply filter blur-2xl opacity-10 animate-pulse">
        </div>
        <div
            class="absolute bottom-0 right-0 w-32 h-32 bg-violet-500 rounded-full mix-blend-multiply filter blur-2xl opacity-10 animate-pulse delay-1500">
        </div>
    </div>

    <div class="w-full max-w-md bg-[#1e1e1e] rounded-lg shadow-2xl p-8">

        <h1 class="text-2xl font-bold text-red-700 mb-6">Suppression de compte</h1>

        <p class="mb-6 text-gray-400">Êtes-vous sûr de vouloir supprimer votre compte ?</p>

        <div class="mb-6 p-4 bg-[#2a2a2a] border-l-4 border-red-700 rounded">
            <p class="font-bold text-red-700 mb-2">⚠️ Action irréversible</p>
            <p class="text-sm text-gray-400">Toutes vos données seront définitivement supprimées. Avis, commentaires, et autres
                informations associées à votre compte seront perdus.</p>
        </div>

        <div id="verifyContainer" class="space-y-4">
            <input type="hidden" id="email" value="<%= email %>">
            <input type="hidden" id="id" value="<%= id %>">

            <div>
                <label class="block text-gray-300 mb-2 font-bold">Indiquer votre pseudo pour continuer :</label>
                <input type="text" id="username" required
                    class="w-full px-4 py-3 bg-[#2a2a2a] border border-gray-600 rounded-lg text-white placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent"
                    placeholder="Votre pseudo">
                <small id="usernameStatus" class="text-gray-500 text-xs mt-1 block"></small>
            </div>

            <div>
                <label class="block text-gray-300 mb-2 font-bold">Mot de passe :</label>
                <input type="password" id="code" minlength="12" required
                    class="w-full px-4 py-3 bg-[#2a2a2a] border border-gray-600 rounded-lg text-white placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent"
                    placeholder="Entrez votre mot de passe">
                <small class="text-gray-500 text-xs mt-1 block">Votre mot de passe actuel</small>
            </div>

            <button id="deleteBtn"
                class="w-full py-3 px-6 bg-red-700 hover:bg-red-800 text-white font-bold rounded-lg transition-colors duration-200 mt-4">
                Confirmer la suppression
            </button>
        </div>

        <button onclick="window.location.href='/'"
            class="w-full py-3 px-6 bg-blue-800 hover:bg-blue-700 text-gray-900 font-bold rounded-lg transition-colors duration-200 mt-4">
            Annuler
        </button>
    </div>

    <script>
        document.getElementById('deleteBtn').addEventListener('click', async () => {
            const email = document.getElementById('email').value.trim();
            const id = document.getElementById('id').value.trim();
            const username = document.getElementById('username').value.trim();
            const code = document.getElementById('code').value.trim();

            if (!username || !code || !email || !id) {
                alert("Veuillez remplir tous les champs.");
                return;
            }

            try {
                const res = await fetch('/api/auth/delete-account', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({ email, id, username, code })
                });

                const data = await res.json();

                if (data.success) {
                    console.log(data.message)
                    window.location.reload()
                } else {
                    alert("❌ Erreur : " + (data.message || "Suppression échouée. Veuillez réessayer."));
                }
            } catch (err) {
                console.error(err);
                alert("⚠️ Erreur réseau");
            }
        });
    </script>
</body>

</html>